# Blockquote with Code Blocks

Blockquotes containing code blocks and lists as nested children.

## Code Inside Quote

> Here is a quoted snippet:
>
> ```python
> def hello():
>     print("world")
> ```
>
> End of quoted snippet.

## Multiple Blocks in Quote

> Instructions to follow:
>
> ```bash
> npm install
> npm run build
> ```
>
> Then check the output:
>
> ```json
> {"status": "ok"}
> ```

## Plain Quote After

> A simple blockquote with **bold** and `code` for contrast.
